# requirements.txt

# --- Core do Pipeline ALPR ---
opencv-python-headless
numpy
sqlalchemy
paddleocr
paddlepaddle # Dependência principal do PaddleOCR
shapely # Para métricas de IoU nos scripts de avaliação

# --- API Backend ---
fastapi
uvicorn[standard] # Servidor ASGI (o [standard] inclui websockets)
pydantic # Para validação de modelos de dados da API
python-multipart # Necessário para UploadFile no FastAPI

# --- Testes e Scripts Auxiliares ---
tqdm # Para barras de progresso nos scripts de avaliação
websockets # Para o script de teste do cliente (test_websocket.py)

# --- Frontend (Legado/Debug) ---
streamlit # Para suas páginas de debug (debugPage, etc.)